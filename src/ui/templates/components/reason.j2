{% macro reason(label) -%}
<div class="flex items-center gap-3">
  <img class="w-4" alt="Circle Check Icon" src="{{url_for('static', filename='images/circle-check-regular.svg')}}" />
  <span class="text-gray-700">{{ label }}</span>
</div>
{%- endmacro %}